*.log
.DS_Store

.env
.env.production
# Filled-in copy of .do/app.yaml with real secrets — used locally with `doctl
# apps create/update --spec`, never committed (the tracked .do/app.yaml keeps
# REPLACE_ME placeholders instead).
/.do/app.production.yaml
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail

Homestead.json
Homestead.yaml
Thumbs.db

# Thư mục clone lồng bị dính nhầm vào git (repo con có .git riêng bên trong repo này)
/Library_Management_System
